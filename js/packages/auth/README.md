# @qlever-llc/trellis-auth

Authentication helpers for Trellis browser, service, admin, and workload-activation clients.

Includes session key management, request signing, browser login/callback helpers, workload activation helpers, and shared auth schemas.

Pairs with `@qlever-llc/trellis`, `@qlever-llc/trellis-svelte`, and the generated SDK packages. See [Trellis Auth Design](../../../design/auth/trellis-auth.md).
