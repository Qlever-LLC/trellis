{
  "extends": "../../deno.json",
  "name": "@qlever-llc/trellis-service-trellis",
  "version": "0.7.0",
  "exports": "./main.ts",
  "tasks": {
    "dev": "deno task prepare && deno task -c ../../portals/login/deno.json build:static:prebuilt && deno run --env --allow-env --allow-sys --allow-read --allow-net --allow-ffi --watch main.ts",
    "prepare": "bash ../../../scripts/trellis-generate.sh",
    "verify:contracts": "deno task prepare",
    "test": "deno task prepare && deno test --allow-env --allow-sys --allow-read --allow-write"
  }
}
