{
  "extends": "../../deno.json",
  "name": "@qlever-llc/trellis-service-jobs",
  "version": "0.6.1",
  "exports": "./mod.ts",
  "tasks": {
    "prepare": "deno task -c ../../deno.json prepare",
    "verify:contracts": "deno task prepare",
    "check": "deno task prepare && deno check source_emission_test.ts",
    "test": "deno task prepare && deno test -A source_emission_test.ts"
  }
}
