{
  "extends": "../../deno.json",
  "name": "@qlever-llc/trellis-service-activity",
  "version": "0.7.0",
  "exports": "./main.ts",
  "tasks": {
    "dev": "deno task prepare && deno run --env --allow-env --allow-sys --allow-read --allow-net --allow-ffi --watch main.ts",
    "prepare": "bash ../../../scripts/trellis-generate.sh",
    "verify:contracts": "deno task prepare",
    "check": "deno task -c ../../deno.json prepare && deno check main.ts bootstrap.ts projectors.ts store.ts contracts/trellis_activity.ts",
    "test:contracts": "deno task prepare && deno test -A source_emission_test.ts"
  }
}
