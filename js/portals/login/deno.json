{
  "extends": "../../deno.json",
  "tasks": {
    "prepare": "deno task -c ../../deno.json prepare",
    "dev": "deno task prepare && vite dev",
    "build:static": "deno task prepare && svelte-kit sync && vite build",
    "build:static:prebuilt": "svelte-kit sync && vite build",
    "build": "deno task prepare && vite build",
    "preview": "deno task prepare && vite preview",
    "check": "deno task prepare && svelte-kit sync && svelte-check --tsconfig ./tsconfig.check.json"
  },
  "compilerOptions": {
    "types": [
      "npm:vite/client"
    ]
  }
}
