{
  "extends": "../../deno.json",
  "tasks": {
    "verify:contracts": "deno task -c ../../deno.json prepare",
    "dev": "deno task verify:contracts && vite dev",
    "build:static": "deno task verify:contracts && svelte-kit sync && vite build",
    "build:static:prebuilt": "svelte-kit sync && vite build",
    "build": "deno task verify:contracts && vite build",
    "preview": "vite preview",
    "check": "deno task verify:contracts && svelte-kit sync && svelte-check --tsconfig ./tsconfig.check.json"
  },
  "compilerOptions": {
    "types": ["npm:vite/client"]
  }
}
