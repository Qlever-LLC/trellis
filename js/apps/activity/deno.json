{
  "extends": "../../deno.json",
  "tasks": {
    "dev": "deno task -c ../../deno.json build:contracts && deno task -c ../../services/activity/deno.json build:sdk && vite dev",
    "build": "deno task -c ../../deno.json build:contracts && deno task -c ../../services/activity/deno.json build:sdk && vite build",
    "preview": "vite preview",
    "check": "deno task -c ../../deno.json build:contracts && deno task -c ../../services/activity/deno.json build:sdk && svelte-kit sync && svelte-check --tsconfig ./tsconfig.check.json"
  },
  "compilerOptions": {
    "types": ["npm:vite/client"]
  }
}
